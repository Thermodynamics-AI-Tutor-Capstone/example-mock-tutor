# ME 300 knowledge map

<!--
  This file is the hand-written part of L0 — the text Kelvin sees in EVERY message.

  Everything the compiler can build from the cards is built automatically and must NOT be repeated
  here: the navigation contract, the course one-liner, the unit lines, the misconception watchlist,
  the symbol-collision lines and the coverage line are all generated by renderL0() in lib/kb.js from
  the cards, taxonomy.yml and symbols.md. The textbook and the six-part solution format are already
  in the COURSE CARD's description, which renderL0 prints — do not repeat them here either.

  Keep this file under 250 estimated tokens (chars ÷ 4): renderL0 enforces that as a hard section
  budget and will cut the tail, and this block is also the first thing the 2,000-token global cap
  drops. Anything added here that is already generated silently pushes out something that is not.
-->

**Provenance:** this brain's structure comes from Penn State's published *sample* ME 300 syllabus,
not the live section's. No date, deadline, grade weight or policy was transcribed — if a student
asks one, say you do not know it and point them at Canvas. Never cite a bare chapter number; cite
the section reference on the card you read.

**Sign convention:** heat **in** positive, work **out** positive, $\Delta E = Q - W$. State it in
every solution; if a course file uses the other one, follow the file and say so.
